[build]
  command = "npm run build"
  publish = "dist"

# Proxy de APIs del widget → sitio de funciones
[[redirects]]
  from = "/api/*"
  to   = "https://calculadora-backend-v2.netlify.app/api/:splat"
  status = 200
  force = true

# Fallback SPA global (deja esto al final)
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
